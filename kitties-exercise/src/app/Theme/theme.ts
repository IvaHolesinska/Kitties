import { DefaultTheme } from 'styled-components'

export const theme: DefaultTheme = {
  colors: {
    white: '#FFFFFF',
    textPrimary: '#333',
    darkGray: '#555',
    gray: '#666',
    lightGray: 'rgb(240, 242, 245)',
  },
  shadow: {
    boxShadow: '0 4px 8px rgba(0, 0, 0, 0.1)',
    textShadow: '2px 2px 4px rgba(0, 0, 0, 0.3)',
  },
}

export default theme
